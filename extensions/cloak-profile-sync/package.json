{
  "name": "cloak-profile-sync",
  "private": true,
  "type": "module",
  "description": "Chrome extension sources — load unpacked via manifest.json, not npm.",
  "scripts": {
    "test": "node --test test/*.test.js"
  }
}
